{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Install and activate pipenv virtual environment on startup",
        "type": "shell",
        "command": "pipenv install -dev && pipenv shell",
        "presentation": {
          "reveal": "always",
          "panel": "new"
        },
        "runOptions": { "runOn": "folderOpen" }
      }
    ]
  }
